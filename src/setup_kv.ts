import { setConfigValue } from './config.ts';

await setConfigValue('DID', 'your_did_here');
await setConfigValue('SIGNING_KEY', 'your_signing_key_here');
await setConfigValue('BSKY_HANDLE', 'your_bsky_handle');
await setConfigValue('BSKY_PASSWORD', 'your_bsky_password');
